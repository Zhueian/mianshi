
 1。说说reids中的项目，缓存、黑白名单、
        防止业务顺序绕过（eg实名：先发短信、在确认短信认证码、再输入旧密码、再改新密码、再绑定身份证、再ocr确认）、
        分布式锁、二级索引、
        预热、（子应用热点省市区、热点安卓版本、做前一天热点的key zset、再用定时任务加载热子应用）

 2。redis是单线程还是多线程：work thread就一个单线程，高版本6.x做了iothread多线程多路复用跟到内核态 和 零拷贝。
    有点想netty的recator模型boss thread 和 work threads，boss thread死循环监听网络事件。
    延伸：dubbo交互都是netty多线程的，但是同步返回怎么确认是哪个线程呢：用有个全局唯一的threadId。

 3。双写一致性，个人感觉缓存就是不严谨的，不能做终态数据的持久化，展示型缓存最好加过期时间